package com.app.urlshortner.dto;

/**
 * Data Transfer Object for outbound responses (e.g., returning the shortened URL back to the user).
 */
public class UrlResponseDto {
    
    private String originalUrl;
    private String shortUrl;
    
    // Add getters and setters here later
}
