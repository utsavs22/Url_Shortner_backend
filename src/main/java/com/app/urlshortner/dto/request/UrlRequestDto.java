package com.app.urlshortner.dto;

/**
 * Data Transfer Object for incoming requests (e.g., when a user submits a long URL to be shortened).
 */
public class UrlRequestDto {
    
    private String originalUrl;
    
    // Add getters and setters here later
}
