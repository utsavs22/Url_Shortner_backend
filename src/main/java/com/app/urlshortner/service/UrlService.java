package com.app.urlshortner.service;

import org.springframework.stereotype.Service;

/**
 * Service class that contains the business logic for shortening URLs and finding original URLs.
 */
@Service
public class UrlService {
    
    // Add logic here later: logic to generate short link, fetch original link, etc.
}
